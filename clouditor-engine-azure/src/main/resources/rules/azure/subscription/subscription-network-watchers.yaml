name: "Azure Network Watcher: Enabled for all regions"
description: "Checks if Azure Network Watcher is enabled for all regions in the current subscription."
condition: Subscription has enabled == true in all watchers
controls:
  - "CIS Microsoft Azure Foundations Benchmark/Azure 6.5"
