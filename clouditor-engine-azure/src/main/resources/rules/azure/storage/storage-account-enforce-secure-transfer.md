# Force Secure Transfer

Checks if Azure StorageAccount only supports secure transfer.

```ccl
StorageAccount has properties.supportsHttpsTrafficOnly == true
````

## Controls

* CIS Microsoft Azure Foundations Benchmark/Azure 3.1
